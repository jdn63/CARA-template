// This is a placeholder that prevents console errors on pages that don't use the map.
// Modal functions (showHercStats, showWemStats, closeHercStats, closeWemStats) are
// the canonical implementations defined in modules/navigation.js and registered as
// window-level globals via its DOMContentLoaded listener. Do not redefine them here.
console.log('Main.js loaded - generic version');
